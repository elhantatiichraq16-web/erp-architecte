{% extends 'base.html.twig' %}

{% block title %}Nouvel événement{% endblock %}

{% block breadcrumb %}
    <li class="breadcrumb-item"><a href="{{ path('app_event_index') }}">Calendrier</a></li>
    <li class="breadcrumb-item active">Nouvel événement</li>
{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-xl-7">
        <div class="card rounded-4 shadow-sm">
            <div class="card-header bg-white py-3">
                <h5 class="mb-0 fw-semibold">
                    <i class="bi bi-calendar-plus me-2 text-primary"></i> Nouvel événement
                </h5>
            </div>
            <div class="card-body p-4">
                {% include 'event/_form.html.twig' %}
            </div>
            <div class="card-footer bg-white border-top d-flex justify-content-between py-3 px-4">
                <a href="{{ path('app_event_index') }}" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left me-1"></i> Annuler
                </a>
                <button type="submit" form="{{ form.vars.id }}" class="btn btn-primary">
                    <i class="bi bi-check-lg me-1"></i> Créer l'événement
                </button>
            </div>
        </div>
    </div>
</div>
{% endblock %}
